# Core dependencies for local GPU models
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.21.0

# AudioCraft/MusicGen
audiocraft>=1.3.0

# AudioLDM and Stable Audio (via diffusers)
diffusers[audio]>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0

# Optional: For better performance
xformers>=0.0.20  # For attention optimization
